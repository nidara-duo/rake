[package]
name = "rake-domain"
version = "0.1.0"
edition = "2024"
description = "Pure domain types for Rake package manager — zero I/O"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"

[lints.rust]
unsafe_code = "forbid"
